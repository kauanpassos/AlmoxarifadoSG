<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Almoxarifado.App.Views.NovaSolicitacaoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Almoxarifado.App.ViewModels"
    x:DataType="vm:NovaSolicitacaoViewModel"
    BackgroundColor="White"
    NavigationPage.HasNavigationBar="False"
    Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *, Auto">

        <Grid Grid.Row="0" BackgroundColor="#1A336E" Padding="20,45,20,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ImageButton Grid.Column="0" Command="{Binding VoltarCommand}"
                Source="{FontImageSource FontFamily=OpenSansSemibold, Glyph='❮', Size=24, Color=White}"
                BackgroundColor="Transparent" VerticalOptions="Center" WidthRequest="40" HeightRequest="40" />

            <Label Grid.Column="1" Text="Solicitação" TextColor="White" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />

            <Frame Grid.Column="2" WidthRequest="40" HeightRequest="40" CornerRadius="20" BackgroundColor="White" Padding="0" HasShadow="False">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding IrParaPerfilCommand}" />
                </Frame.GestureRecognizers>
                <Label Text="{Binding IniciaisUsuario}" TextColor="#1A336E" FontAttributes="Bold" FontSize="15" HorizontalOptions="Center" VerticalOptions="Center" />
            </Frame>
        </Grid>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="31,25,31,30" Spacing="25">
                <Frame BackgroundColor="White" BorderColor="#254A9A" CornerRadius="5" HasShadow="True" Padding="15,19">
                    <VerticalStackLayout>
                        <Label Text="Selecione a Peça" TextColor="Black" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15" />
                        <Frame BackgroundColor="#F1F1F1" BorderColor="#757575" CornerRadius="20" Padding="15,5" HasShadow="False" Margin="0,0,0,15">
                            <Grid ColumnDefinitions="Auto,*">
                                <Label Text="🔍" VerticalOptions="Center" FontSize="16" />
                                <Entry Grid.Column="1" Placeholder="Pesquisar por Peça, SKU ou Status" PlaceholderColor="#757575" TextColor="Black" BackgroundColor="Transparent" FontSize="14" />
                            </Grid>
                        </Frame>
                        <Label Text="Sugestões rápidas" TextColor="Black" FontSize="14" FontAttributes="Bold" Margin="20,0,0,5" />
                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding SugestoesRapidas}">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Label Text="{Binding .}" TextColor="Black" FontSize="12" FontAttributes="Bold" Margin="35,0,0,5" />
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <Frame BackgroundColor="White" BorderColor="#254A9A" CornerRadius="5" HasShadow="True" Padding="25,15">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Quantidade" TextColor="#1A1A1A" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
                        <Grid ColumnDefinitions="Auto, *, Auto" VerticalOptions="Center" Padding="0,5">
                            <Button Grid.Column="0" Command="{Binding DiminuirQuantidadeCommand}" Text="-" FontSize="24" FontAttributes="Bold" TextColor="Black" BackgroundColor="#D9D9D9" CornerRadius="10" WidthRequest="50" HeightRequest="50" Padding="0" />
                            <Label Grid.Column="1" Text="{Binding QuantidadeSolicitada, StringFormat='{0} un'}" TextColor="#254A9A" FontSize="25" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />
                            <Button Grid.Column="2" Command="{Binding AumentarQuantidadeCommand}" Text="+" FontSize="24" FontAttributes="Bold" TextColor="Black" BackgroundColor="#D9D9D9" CornerRadius="10" WidthRequest="50" HeightRequest="50" Padding="0" />
                        </Grid>
                        <Label Text="{Binding EstoqueDisponivel, StringFormat='Disponível: {0}'}" TextColor="Black" FontSize="14" FontAttributes="Bold" HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </Frame>

                <Frame BackgroundColor="White" BorderColor="#254A9A" CornerRadius="5" HasShadow="True" Padding="15">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Justificativa" TextColor="Black" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
                        <Frame BackgroundColor="#E2E2E2" CornerRadius="15" BorderColor="Transparent" Padding="10" HeightRequest="100" HasShadow="False">
                            <Editor Placeholder="Descreva a necessidade da peça ..." PlaceholderColor="#757575" TextColor="Black" BackgroundColor="Transparent" AutoSize="TextChanges" />
                        </Frame>
                    </VerticalStackLayout>
                </Frame>

                <Button Command="{Binding ConfirmarPedidoCommand}" Text="Confirmar Pedido" TextColor="White" BackgroundColor="#254A9A" FontAttributes="Bold" FontSize="20" CornerRadius="25" HeightRequest="65" Margin="0,10,0,20" />
            </VerticalStackLayout>
        </ScrollView>
        <BoxView Grid.Row="2" HeightRequest="49" Color="#1A336E" />
    </Grid>
</ContentPage>